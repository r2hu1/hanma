name: monitoring-sentry
category: feature
featureType: monitoring
description: Sentry error tracking and performance monitoring
framework: express

includes:
  - snippet: express/v5/libs/sentry.hbs
    path: src/libs/sentry.ts

dependencies:
  - "@sentry/node@^9.33.0"

envVars:
  - SENTRY_DSN=
  - NODE_ENV=development

merge:
  - file: package.json
    strategy: deep
