name: mailer-sendgrid
category: feature
featureType: mailer
description: Email sending with SendGrid API

includes:
  - snippet: shared/mailers/sendgrid.hbs
    path: src/libs/mailer.ts

dependencies:
  - "@sendgrid/mail@^8.1.5"

envVars:
  - SENDGRID_API_KEY=
  - MAIL_FROM=noreply@example.com

merge:
  - file: package.json
    strategy: deep
