name: better-auth
category: auth
description: better-auth setup with session management
framework: express

dependencies:
  - better-auth@^1.2.12

devDependencies: []

envVars:
  - BETTER_AUTH_SECRET
  - BETTER_AUTH_URL

merge:
  - file: package.json
    strategy: deep
